# F3-01 paginación de tickets — Implementation Plan

> REQUIRED SUB-SKILL: subagent-driven-development. Trabajo directo en `main` (commits locales; push al final).

**Goal:** Vistas activas cargan `getActiveTickets` (acotado); vista `cerrados` pagina contra el server (page+total+controles). Sin cambiar el comportamiento de las vistas activas.

**Spec:** `docs/superpowers/specs/2026-06-19-f3-01-paginacion-design.md`.

---

## Task 1: Backend — closed paginado (TDD)

**Files:** `packages/zoho-sync/src/db/repo.ts`, `packages/zoho-sync/src/db/repo.test.ts`, `apps/desk/server/app.ts`, `apps/desk/server/app.test.ts`.

- [ ] **Step 1: Tests repo (repo.test.ts)** — sembrar mezcla activos/cerrados (status_type 'Closed' vs 'Open') y verificar:
  - `getClosedTickets(db, '', 2, 0)` → solo cerrados, máx 2, orden por created_time desc.
  - `getClosedTickets(db, '', 2, 2)` → la siguiente página.
  - `countClosedTickets(db)` → nº de cerrados (no cuenta activos).
- [ ] **Step 2: Run** → FAIL.
- [ ] **Step 3: Implementar en `repo.ts`** (junto a `getActiveTickets`, reusando `mapTicketRowWithRefs`):
```ts
export async function getClosedTickets(db: Queryable, userId = '', limit = 50, offset = 0): Promise<TicketWithRefs[]> {
  const r = await db.query(
    `SELECT t.*, COALESCE(a.name, cl.name) AS account_name, g.name AS agent_name, c.first_name AS c_first, c.last_name AS c_last, tr.read_at
     FROM tickets t LEFT JOIN accounts a ON t.account_id=a.id LEFT JOIN agents g ON t.assignee_id=g.id
     LEFT JOIN clients cl ON t.client_id=cl.id
     LEFT JOIN contacts c ON t.contact_id=c.id
     LEFT JOIN ticket_reads tr ON tr.ticket_id=t.id AND tr.user_id=$1
     WHERE t.status_type = 'Closed' ORDER BY t.created_time DESC NULLS LAST LIMIT $2 OFFSET $3`,
    [userId, limit, offset],
  )
  return r.rows.map(mapTicketRowWithRefs)
}

export async function countClosedTickets(db: Queryable): Promise<number> {
  const r = await db.query(`SELECT count(*)::int AS n FROM tickets WHERE status_type = 'Closed'`)
  return r.rows[0]?.n ?? 0
}
```
- [ ] **Step 4: Tests endpoint (app.test.ts)** — con `appWith()` + sembrar activos/cerrados:
  - `GET /api/tickets` (sesión) → array, **solo activos** (ningún `statusType==='Closed'`).
  - `GET /api/tickets?scope=closed&page=1` → `{ items, total, page, pageSize }`, `items` solo cerrados, `items.length <= pageSize`, `total` = nº cerrados, `page===1`, `pageSize===50`.
  - `?scope=closed&page=99` → `items` vacío, `total` correcto.
- [ ] **Step 5: Run** → FAIL.
- [ ] **Step 6: Implementar el endpoint en `app.ts`** — reemplazar el handler `GET /api/tickets` actual por:
```ts
  app.get('/api/tickets', asyncHandler(async (req, res) => {
    if (req.query.scope === 'closed') {
      const pageSize = 50
      const page = Math.max(1, Number(req.query.page) || 1)
      const items = await getClosedTickets(db, req.user!.id, pageSize, (page - 1) * pageSize)
      const total = await countClosedTickets(db)
      res.json({ items: items.map(({ row, refs }) => rowToTicket(row, refs)), total, page, pageSize })
      return
    }
    // scope=all (compat, sin uso en el front) o default (active)
    const list = req.query.scope === 'all' ? await getAllTickets(db, req.user!.id) : await getActiveTickets(db, req.user!.id)
    res.json(list.map(({ row, refs }) => rowToTicket(row, refs)))
  }))
```
Añadir `getClosedTickets, countClosedTickets` al import de `@ambientalia/zoho-sync/db/repo`.
- [ ] **Step 7: Run** `npm test` → verde. `npm run typecheck` exit 0. `npm run lint` sin errores.
- [ ] **Step 8: Commit (local, main):** `git add packages/zoho-sync/src/db/repo.ts packages/zoho-sync/src/db/repo.test.ts apps/desk/server/app.ts apps/desk/server/app.test.ts && git commit -m "feat(desk): backend paginación de tickets cerrados (F3-01)"` (trailer Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>). NO push.

---

## Task 2: Frontend — fetch por vista + controles de paginación

**Files:** `apps/desk/src/api/client.ts`, `apps/desk/src/components/Pagination.tsx` (nuevo), `apps/desk/src/App.tsx`.

- [ ] **Step 1: `api/client.ts`** — añadir:
```ts
export function fetchActiveTickets(): Promise<Ticket[]> {
  return fetch('/api/tickets', { credentials: 'include' }).then((r) => json<Ticket[]>(r))
}
export interface ClosedPage { items: Ticket[]; total: number; page: number; pageSize: number }
export function fetchClosedTickets(page: number): Promise<ClosedPage> {
  return fetch(`/api/tickets?scope=closed&page=${page}`, { credentials: 'include' }).then((r) => json<ClosedPage>(r))
}
```
(Conservar `fetchTickets` si algún test lo usa; si no, se puede dejar.)
- [ ] **Step 2: `components/Pagination.tsx`** (presentacional):
```tsx
export function Pagination({ page, pageSize, total, onPrev, onNext }: { page: number; pageSize: number; total: number; onPrev: () => void; onNext: () => void }) {
  const pages = Math.max(1, Math.ceil(total / pageSize))
  return (
    <div className="flex items-center justify-center gap-3 py-3 text-[13px] text-slate-600">
      <button className="px-3 py-1 rounded border border-slate-300 disabled:opacity-40" disabled={page <= 1} onClick={onPrev}>Anterior</button>
      <span>Página {page} de {pages} · {total} cerrados</span>
      <button className="px-3 py-1 rounded border border-slate-300 disabled:opacity-40" disabled={page >= pages} onClick={onNext}>Siguiente</button>
    </div>
  )
}
```
- [ ] **Step 3: `App.tsx`** — flujo por vista:
  - Importar `fetchActiveTickets, fetchClosedTickets, type ClosedPage` y `Pagination`.
  - Estado: `const [closedPage, setClosedPage] = useState(1)`; reset al cambiar de vista: `useEffect(() => { setClosedPage(1) }, [view])`.
  - Reemplazar `const { data: tickets, loading, error, reload } = useAsync(() => fetchTickets('all'), [user?.id])` por:
    ```tsx
    const isClosed = view === 'cerrados'
    const { data: resp, loading, error, reload } = useAsync(
      () => (isClosed ? fetchClosedTickets(closedPage) : fetchActiveTickets()),
      [user?.id, view, closedPage],
    )
    const tickets: Ticket[] = isClosed ? ((resp as ClosedPage | undefined)?.items ?? []) : ((resp as Ticket[] | undefined) ?? [])
    const closedMeta = isClosed ? (resp as ClosedPage | undefined) : undefined
    ```
    (Ajustar los tipos de `useAsync` si hace falta; `resp` es `Ticket[] | ClosedPage`.)
  - Renderizar `<Pagination>` cuando `isClosed && closedMeta` (debajo del listado/board): `page={closedPage} pageSize={closedMeta.pageSize} total={closedMeta.total} onPrev={() => setClosedPage(p => Math.max(1, p-1))} onNext={() => setClosedPage(p => p+1)}` (acotar next con `Math.ceil(total/pageSize)`).
  - El resto (applyBoardView, KanbanBoard/TicketList/TicketTable) se mantiene leyendo `tickets`/`baseRead`.
- [ ] **Step 4: Verificar:** `npm run typecheck` exit 0; `npm run lint` sin errores; `cd apps/desk && npx vite build` OK; `npm test` verde.
- [ ] **Step 5: Commit (local, main):** `git add apps/desk/src/api/client.ts apps/desk/src/components/Pagination.tsx apps/desk/src/App.tsx && git commit -m "feat(desk): vista 'cerrados' paginada + tablero carga solo activos (F3-01)"` (trailer). NO push.

---

## Task 3: Verificación final + push
- [ ] `npm test && npm run typecheck && npm run lint && (cd apps/desk && npx vite build)` → todo verde.
- [ ] `git push origin main` (si la branch protection lo rechaza → el usuario la pone en Disabled, ver nota).
- [ ] Validar tras deploy: vistas activas iguales/rápidas; `cerrados` pagina 50/pág con controles; total correcto.

## Notas
- `getActiveTickets` ya filtra `status_type <> 'Closed'` → las 4 vistas activas dan el mismo resultado que hoy.
- pageSize=50 fijo (config futura si se necesita).
- Si `useAsync` no tipa bien la unión, castear como en el Step 3.
