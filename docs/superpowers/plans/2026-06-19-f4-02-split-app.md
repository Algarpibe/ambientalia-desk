# F4-02 partir app.ts — Implementation Plan

> REQUIRED SUB-SKILL: subagent-driven-development. Directo en `main`. **Invariante:** refactor SIN cambio funcional; los 259 tests verdes tras CADA tarea (son el contrato). Spec: `docs/superpowers/specs/2026-06-19-f4-02-split-app-design.md`.

Cada tarea: extraer → `npm test` (259 verde) + `npm run typecheck` (0) + `npm run lint` (0 err) → commit local en `main` (trailer Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>). Push al final.

---

## Task 1: utils — asyncHandler + HttpError + branch en el error-handler
**Files:** Create `apps/desk/server/util/asyncHandler.ts`, `apps/desk/server/util/httpError.ts`; Modify `apps/desk/server/app.ts`.
- Mover `asyncHandler` (de app.ts:41) a `util/asyncHandler.ts` (`export const asyncHandler = ...`); importarlo en app.ts.
- `util/httpError.ts`: `export class HttpError extends Error { constructor(public status: number, public body: unknown) { super(typeof body === 'object' && body && 'error' in body ? String((body as any).error) : 'HttpError') } }`.
- En el error-handler central de app.ts, añadir AL INICIO: `if (err instanceof HttpError) { res.status(err.status).json(err.body); return }` (antes del `console.error`+500). Importar `HttpError`.
- Verificar (259 verde, tsc, lint). Commit: `refactor(desk): util asyncHandler + HttpError + mapeo en error-handler (F4-02)`.

## Task 2: services/ticketService.ts (create + transition)
**Files:** Create `apps/desk/server/services/ticketService.ts`; Modify `apps/desk/server/app.ts`.
- `createManagedTicket(db, body, actorName)`: TODA la lógica del `POST /api/tickets` actual (app.ts:128-166), lanzando `throw new HttpError(422, { error })` en cada validación; devuelve el `rowToTicketDetail(...)` creado. Importa getEquipo/getSalesOrder/getClient (books/db) + createTicket/getTicketWithRefs + rowToTicketDetail + buildSubject/buildCodigoServicio/PREFIJOS.
- `executeTransition(db, id, body, user)`: lógica del `POST /api/tickets/:id/transition` (app.ts:359-380): transitionById→`HttpError(400,...)`, getTicketWithRefs→`HttpError(404,...)`, from-check→`HttpError(409,...)`, canExecuteTransition→`HttpError(403,...)`, buildTransitionPlan errors→`HttpError(422,{errors})`, applyTransition; devuelve `rowToTicketDetail` actualizado. `user` = `{ areas, isAdmin, name }`.
- En app.ts, reemplazar los dos handlers por: `res.status(201).json(await createManagedTicket(db, req.body, req.user?.name ?? 'App'))` y `res.json(await executeTransition(db, String(req.params.id), req.body, req.user!))`.
- (Opcional barato) `services/ticketService.test.ts` con pg-mem: createManagedTicket lanza HttpError sin equipo; transition 404 si no existe.
- Verificar (259+ verde) — **mismos status/bodies** (los tests de create/transition de app.test.ts lo prueban). Commit: `refactor(desk): capa de servicio ticketService (create + transition) (F4-02)`.

## Task 3: routes/tickets.ts
**Files:** Create `apps/desk/server/routes/tickets.ts`; Modify `apps/desk/server/app.ts`.
- `export function registerTicketRoutes(app, deps)` con `deps = { db, sync, zohoFetch, config }`. Mover TODO lo de `/api/tickets/*`: el `app.use('/api/tickets', requireAuth(db))`, multer `upload`, resolución (get/put/post + delete×2 requireSuperAdmin), `GET /api/tickets` (paginación), `POST /read`, `POST /` (→ createManagedTicket), `GET /:id`/conversations/history/activities (lazy+background idénticos), `POST /:id/transition` (→ executeTransition), `POST /:id/reply` (guardWrites). `ALLOWED_IMAGE_TYPES` + `guardWrites` van con este router (o util). Importa requireAuth/requireSuperAdmin, asyncHandler, repos, etc.
- En app.ts: quitar esas rutas; llamar `registerTicketRoutes(app, { db, sync, zohoFetch, config })`.
- Verificar (259 verde). Commit: `refactor(desk): routes/tickets.ts (F4-02)`.

## Task 4: routes/{directory,equipos,analisis,admin,attachment}.ts
**Files:** Create los 5; Modify `apps/desk/server/app.ts`.
- **directory.ts** `registerDirectoryRoutes(app,{db})`: clients, sales-orders, contacts(+:id), accounts(+:id), activities (cada uno con `requireAuth(db)`).
- **equipos.ts** `registerEquipoRoutes(app,{db})`: equipos list/manage/facets/:id/historial/create/patch/delete(requireSuperAdmin).
- **analisis.ts** `registerAnalisisRoutes(app,{db})`: `/api/analisis` (requireAuth+requireSuperAdmin).
- **admin.ts** `registerAdminRoutes(app,{db,sync,measurer,detailBackfiller})`: measure-attachments, backfill-details, backfill-serial, backfill-archived (requireAuth+requireSuperAdmin). `humanBytes` → mover aquí o a util.
- **attachment.ts** `registerAttachmentRoutes(app,{db,zohoFetch})`: `/api/attachment` (requireAuth, regex SSRF intacta).
- En app.ts: quitar esas rutas; llamar los register*. `measurer`/`detailBackfiller` se crean en createApp y se pasan a admin.
- Verificar (259 verde). Commit: `refactor(desk): routes directory/equipos/analisis/admin/attachment (F4-02)`.

## Task 5: app.ts fino + verificación total + push
**Files:** `apps/desk/server/app.ts`.
- `createApp` debe quedar: express + trust proxy + helmet + json + cookieParser + rate-limit + registerAuthRoutes + crear measurer/detailBackfiller + los 6 register* + error-handler central. Eliminar imports ya no usados (mover los necesarios a sus routers).
- Verificación total: `npm test` (259) + `npm run typecheck` + `npm run lint` + `(cd apps/desk && npx vite build)`.
- `git push origin main`.
- Validar tras deploy: la app funciona idéntica (login, tablero, crear ticket, transición, reply, equipos, análisis, admin).

## Notas
- NO cambiar status/bodies de ninguna ruta. Si un test falla, es señal de que la extracción cambió comportamiento → arreglar la extracción, no el test.
- `req.user` lo inyecta `requireAuth`; los routers que lo usan deben ir tras ese middleware (igual que hoy).
- Imports que se mueven: limpiar los que queden sin uso en app.ts (lint los marca).
