# Parte de supervisión · 2026-09-24 (tercer corte del día, 15:46 UTC)

**Medido contra `71499ba`** (main, 24/09). Corte anterior: `docs/sdd/Parte_2026-09-24.md` (13:59). Tercer corte del mismo día porque Gerencia siguió escribiendo en el panel después de él; el fichero anterior **no se toca**.

---

## 1 · Lo que Alfonso respondió y anotó desde el corte anterior, y dónde quedó

**Tres respuestas y una corrección.** Las tres respuestas cierran las tres preguntas que el corte de las 13:38 devolvió sobre el equipo. La corrección es a `decision/fecha-corte`, y es lo más grande del día.

| Qué | Dónde quedó, fichero a fichero |
|---|---|
| **`equipo-nuevo-alta-en-ticket`** — el alta del ticket de «Equipo nuevo» registra el equipo en el mismo paso. **Fila propia, S, desacoplada de F1B-06** | `openspec/config.yaml` → `decisiones_de_gerencia` · plan `…R01.1.md` §4.5, fila nueva · `docs/sdd/ENTRADA.md` → **E-070, cerrada** · `docs/sdd/R08.3_Expediente_de_cambios.md` §11.4 |
| **`edicion-datos-comerciales-equipo`** — factura, fin de garantía y mantenedor sólo los cambia **Comercial y administradores**; los otros tres, cualquiera con sesión. Historial visible y restricción **en el servidor** | `openspec/config.yaml` → `decisiones_de_gerencia` · plan §4.5, misma fila que la anterior («Alta y edición del equipo», S–M) · `ENTRADA.md` → **E-071, cerrada** · R08.3 §11.4 |
| **`mapa-en-la-app`** — el mapa del blueprint se ve dentro de la aplicación, **antes de F1B-06**, con **talla condicionada a una medición** (XS o S) | `openspec/config.yaml` → `decisiones_de_gerencia` · plan §4.5, fila nueva · `ENTRADA.md` → **E-072, cerrada** · R08.3 §11.4 |
| **Corrección a `fecha-corte`** (tipo `correccion`, opción B) — **el corte se parte en dos tiempos**: 14/12 operativo, independencia total en enero de 2027. Plan A: corte único el **01/02/2027** | `openspec/config.yaml` → `decision/fecha-corte` → **`corregida_por`**, escrito **sin borrar** la decisión original · plan §4.5, fila `decision/fecha-corte` precisada · `ENTRADA.md` → **E-073, ruteada** · R08.3 §11.4 |

Claves Engram que la próxima sesión de construcción debe cargar: **`decision/equipo-nuevo-alta-en-ticket`**, **`decision/edicion-datos-comerciales-equipo`**, **`decision/mapa-en-la-app`** y la relectura de **`decision/fecha-corte`**, que ya no dice lo que decía esta mañana.

**Las cinco quedan marcadas «aterrizada» / «recogida» en el panel**, con sus ficheros. La idea `qm27sclpaheoq43q3k3k` es el **mismo texto** de la corrección, ocho minutos antes: se aterriza una sola vez y se marca como duplicado.

---

## 2 · Lo que la corrección deja abierto — devuelto como pregunta

**`corte-licencias-plan-a`** · El plan A del **01/02/2027 supera el tope del 21/12** que la decisión original fijaba por la renovación de licencias de Zoho. Nadie ha mirado el contrato. **Observado, no medido.**

**`mapa-antes-o-despues-del-corte`** · Dos decisiones del **mismo día** sitúan el mapa a los dos lados del corte: `mapa-en-la-app` lo pone **antes de F1B-06** (que está antes del corte) y la corrección de `fecha-corte` lo pone **después** del corte. No pueden ser ciertas a la vez.

---

## 3 · Reconciliación — las seis comprobaciones, a mano

⚠️ **`npm run reconcile` NO ARRANCA desde esta sesión.** `node_modules/esbuild` está instalado para Windows y esta supervisión corre en Linux. `RECONCILIACION.md` sigue midiendo `aa1962a` (23/09), un commit por detrás. **Comprobar:** `cd C:\dev\Desk_2_R1.023 && npm run reconcile` desde Windows.

| # | Resultado | Comando |
|---|---|---|
| a | **19 declaradas · 12 specs · 0 huérfanas.** Sin cambio | `grep -c "  - name:" openspec/config.yaml` · `ls openspec/specs/*/spec.md` |
| b | **9 derivables de cabecera** (F0-01,02,03,05, F1A-06,07,08, F1B-02, F1B-10) · **0 declarados por commit** — `cierres_declarados_por_commit` sigue sin existir en `config.yaml`. Denominador **52**. Sin cambio | `grep -m1 "^tanda:\|^cierra:" openspec/changes/**/proposal.md` |
| c | **5 fuera-del-plan · 0 sin motivo.** Sin cambio | `grep -A1 "^tanda: fuera-del-plan" openspec/changes/archive/*/proposal.md` |
| d | **4 IV vivos** (IV-8, IV-9, IV-11, IV-12); IV-9 e IV-12 **sin destino a propósito**. **0 gates sin decidir. 6 claves `decision/*` sin fila en §4.5**, las mismas seis | `grep -c "^  - clave:" openspec/config.yaml` = **67** (era 64) |
| e | **estados 21 = maestro 21 ✓ · transiciones 34 = maestro 34 ✓ · esperas código 11 / maestro 4** (divergencia legítima declarada) · **pasos_del_mapa 38 / 36**, sigue sin cerrar pese a F1A-06 | `python3` sobre `packages/shared/src/estados.ts` y `transitions.ts` — **leído del código** |
| f | **5 ficheros de `docs/sdd` sin trackear** (eran 12) · **0 commits sin empujar** · **sin `.git/index.lock`** — el huérfano que denunció el parte anterior **ya no está** | `git ls-files --others --exclude-standard docs/sdd` · `git log origin/main..main` |

---

## 4 · Lo nuevo en el repositorio desde el corte anterior

**Ningún commit nuevo:** `main` sigue en `71499ba`. El avance **no se mueve** por trabajo ni por dictamen.

**Lo que sí apareció, sin commitear: `docs/sdd/Desk2.0_Plan_Fases_y_Tandas_ClaudeCode_R01.3.md`** (sin trackear, 15:28). Es la revisión que **mide el corte del 14/12 y lo declara inviable**: 10,9 semanas disponibles frente a 13,5–15,5 necesarias — **déficit de 3,6 a 5,6 semanas** con el margen que Gerencia exige. Incorpora dos épicas que **no están en el §5 del plan vigente**: **1G** (repatriación del histórico, 4 filas) y **1H** (correo propio, 4 filas). **Comprobar:** `sed -n '95,120p' docs/sdd/Desk2.0_Plan_Fases_y_Tandas_ClaudeCode_R01.3.md`.

**Desvío que esto abre, y se dice sin resolverlo:** la corrección de Gerencia se apoya en 1G y 1H, y esas ocho filas **viven en un fichero sin trackear**. Mientras no estén donde el plan las carga, el corte en dos tiempos está decidido sobre filas que ninguna sesión de construcción ve. No se les asigna destino aquí: eso es alcance.

**Sigue vivo del corte anterior, y vencía hoy:** el fichero con las **tres consultas de sólo lectura** que Alfonso ejecuta mañana 25/09 contra producción. `find . -name "*consulta*" -newer docs/sdd/Parte_2026-09-24.md` = **0**. No existe.

**Sigue vivo:** el campo del **mantenedor** — F1B-02 cerró el 23/09 y `git log --grep=mantenedor` = 0.

---

## 5 · Triaje de `ENTRADA.md`

Entradas nuevas desde el corte anterior: **E-073** (la corrección), ruteada a `config.yaml` → `corregida_por`, plan §4.5 y R08.3 §11.4. **No abre fila.** E-070, E-071 y E-072 pasan de `triada` a **cerrada** con la respuesta de Gerencia. **E-062 sigue sin destino** y nada la ha movido.

---

## 6 · Lo que queda modificado sin commitear — cuenta al presupuesto del ledger de la siguiente tanda

- `openspec/config.yaml` — **+51 líneas** de decisiones nuevas y **+35** del bloque `corregida_por`. ⚠️ **Desplaza las citas `config.yaml:NNNN` posteriores a la línea 2177**, igual que pasó el 24/09 con `4d1f965`. Hay que pasar el detector de citas **antes del próximo push**: `npx tsx apps/desk/server/citas/cli.ts`.
- `docs/sdd/Desk2.0_Plan_Fases_y_Tandas_ClaudeCode_R01.1.md` — 3 filas nuevas en §4.5, la fila `decision/fecha-corte` precisada y la nota del tercer corte. Desplaza citas `plan:NNN` a partir de la línea 377.
- `docs/sdd/ENTRADA.md` — E-070/071/072 cerradas, E-073 nueva.
- `docs/sdd/R08.3_Expediente_de_cambios.md` — §11.4 nueva.
- **Sin trackear:** `Desk2.0_Plan_Fases_y_Tandas_ClaudeCode_R01.3.md`, `Evidencia_Transporte_Tarea_Programada_2026-09-18.txt`, `Parte_2026-09-18.md`, `Parte_2026-09-22.md`, `Parte_2026-09-24.md` y este parte.

Esta supervisión **no ha usado `git add` ni `git commit`**.

---

## 7 · Nota de ejecución — reejecución a mano, 16:15 UTC

Gerencia forzó una segunda ejecución del corte con el encargo de registrar las tres respuestas y la corrección de `fecha-corte`. **Ya estaban registradas** por este mismo parte, 20 minutos antes. Verificado, no rehecho:

| Qué pedía la reejecución | Estado medido |
|---|---|
| Marcar las tres respuestas del panel | **Hechas.** `respuestas` sin ninguna entrada fuera de `estado: aterrizada` (consulta `where estado != aterrizada` = 0 documentos) |
| Registrar la corrección como enmienda de `fecha-corte`, conservando la respuesta original | **Hecha.** `openspec/config.yaml:2178`, clave `corregida_por`; la `respuesta_textual` original **intacta** |
| Actualizar la fila del gate en §4.5 | **Hecha.** Fila `decision/fecha-corte` precisada, y tres filas nuevas |
| No tocar ni commitear la R01.3 | **Cumplido.** `Desk2.0_Plan_Fases_y_Tandas_ClaudeCode_R01.3.md` sin trackear y sin modificar (mtime 15:28, anterior a este corte) |
| No commitear | **Cumplido.** `main` = `origin/main` = `71499ba`; 4 ficheros modificados en el árbol |

**Un defecto propio, encontrado al verificar y corregido.** El `TEXTO LITERAL` que este parte guardó en `corregida_por` era el del **duplicado** `qm27sclpaheoq43q3k3k`, no el del documento canónico: le faltaba la coletilla «(decidida por Gerencia el 24/09/2026, opción B)». La regla exige las palabras de Gerencia, no un resumen ni una variante. **Corregido**, y comprobado carácter a carácter contra el panel. Es el único cambio que deja esta reejecución.

**Nada nuevo de Gerencia** desde las 15:46: ninguna respuesta sin marcar, ninguna idea sin recoger. No se reescribe el calendario de la R01.3 — eso lo hace Claude Code leyendo `config.yaml`.

**Y una nota de fontanería, que corrige lo que este mismo parte dijo antes.** El §3.f midió el árbol **sin** `.git/index.lock`, y era cierto a esa hora: el huérfano que denunció el parte del 24/09 ya no estaba. Pero el `git status` de **esta reejecución** ha creado uno nuevo —`.git/index.lock`, 0 bytes, 16:05— y el montaje no permite retirarlo (`Operation not permitted`). **No se ha borrado, por la regla del parte.** Se borra desde Windows: `del C:\dev\Desk_2_R1.023\.git\index.lock`. Es inocuo mientras nadie escriba en el índice, pero **bloqueará el primer `git add` o `git commit`** que Claude Code intente sobre este árbol.
