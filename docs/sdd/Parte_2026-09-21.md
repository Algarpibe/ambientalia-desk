# Parte de desvíos · lunes 21 de septiembre de 2026 · semana 39

**Medido contra `227624d`** (`main` = `origin/main`, 0 commits sin empujar). **Corte anterior:** `docs/sdd/Parte_2026-09-18.md`.
**`npm run reconcile` existe** (`package.json:20`) y se ha ejecutado; en esta máquina necesita `ESBUILD_BINARY_PATH` porque `node_modules` es de Windows. El diff de `docs/sdd/RECONCILIACION.md` es la base de §3.

## 1 · Lo que Gerencia respondió y anotó, y dónde aterrizó

**Siete respuestas, ninguna idea.** Llegaron por el panel el 17/09 por la noche; el corte del 18/09 no las vio. Las siete están en `openspec/config.yaml` → `decisiones_de_gerencia` con su texto literal, y marcadas **«aterrizada»** en el panel (lote atómico, 7 de 7).

| Respuesta (clave Engram a cargar) | Además aterrizó en | Consecuencia que no preveía |
|---|---|---|
| `decision/ovi-garantia-autor` — «…más concretamente el Director Técnico» | plan §4.5 (decidido) · `ENTRADA.md` E-017 · expediente §11 | La regla va por **cargo** antes de que exista la matriz de cargos (`c10-permisos-cargo`, pendiente). Hipótesis sobre el orden de construcción |
| `decision/p44-escritura-zoho` — no se escribe en Zoho; «espejo» si hace falta | plan §4.5 · E-018 · expediente §11 (nº 44) | **Observado, no medido:** `feat/mark-and-sweep` y `fix/sweep-contacts-all-types` (sin fusionar) hablan de borrados en Books → `git log -3 feat/mark-and-sweep` |
| `decision/titularidad-ov-equipo` — un caso: el mantenedor | plan §4.5 · E-019 · IV-8 en `config.yaml` · expediente §11 | La app no conoce al mantenedor (`grep -rni mantenedor packages/ apps/ --include=*.ts` = 0). **Pregunta nueva** |
| `decision/vigencia-contrato` — no, salvo ampliación dentro del año | plan §4.5 · E-020 · expediente §11 | No es el «no» liso que abría la opción B. «Año» leído como año natural: **interpretación** |
| `decision/escalado-destinatario-doble` — el aviso va al Coordinador Comercial | plan §4.5 (fila del escalado) · E-014 adenda · `precisada_por` | Precisa la del 17/09: **un** destinatario, que ya existe (`packages/shared/src/transitions.ts:278`). La alarma sigue sin construir (`packages/shared/src/sla.ts:32-35`) |
| `decision/e013-staging-f0-04` — no existe copia de pruebas | E-013 adenda (**parcial**) | F0-04 **deja de contar**. No dice si se construye o se retira: **pregunta nueva** |
| `decision/e003c-recuento-modelos` — «En este momento hay 29» | `catalogo-equipos` → `cifra_de_gerencia_2026_09_17` · E-003 adenda | Cifra declarada, no medida. Las otras dos preguntas de E-003 siguen abiertas |

**Regla de redacción aplicada** en los ficheros tocados: 7 menciones quitadas del plan (`grep -ciE "sesi[oó]n del|reuni" <plan>` = 0), 3 de `ENTRADA.md`, el encabezado de B.3 del expediente y `F0-05_Mecanismo_de_Reconciliacion.md:93`.

## 2 · Avance: 11 / 52, con dos motivos que se compensan

| Cifra | Valor | Cómo |
|---|---|---|
| Derivable de cabecera | **5**: F0-01, F0-02, F0-03, F0-05, F1A-08 | `grep -rn "^cierra: si" openspec/changes` menos los dos casos de §3 (D1, D2) |
| Declarada por commit | **6**: F0-00, F1A-01, F1A-02, F1A-04, F1A-05, F1B-01 | Del parte del 18/09 menos F0-04. **No está en `config.yaml`** (`cierres_declarados_por_commit`): reconcile publica 0 |
| Movimiento | **+1 por trabajo** (F0-05, `bd2122f`) · **−1 por dictamen** (F0-04) | Esfuerzo 16,2 %, era 17,6 % (hipótesis: peso 17,5 de 108) |

## 3 · Desvíos, cada uno con su comando

| # | Desvío | Comando | Estado |
|---|---|---|---|
| **D1** | **Reconcile cuenta F0-05 dos veces** (7 por cabecera): queda una copia vieja sin trackear en `openspec/changes/F0-05/` (18/09) junto a la archivada | `git status --short openspec/changes/` · `grep -n "derivables" docs/sdd/RECONCILIACION.md` | **nuevo** |
| **D2** | **Reconcile cuenta F1B-10**, que no está en `main`: cuenta todo `cierra: si`, archivado o no (`apps/desk/server/reconciliacion/comprobaciones.ts:203-207`). La rama va **14 commits** por delante, con verify R1 FAIL (`91d026f`) y R2 reevaluado (`7bf0626`) | `git log main..f1b-10-r1 --oneline \| wc -l` | **nuevo** |
| **D3** | **`RECONCILIACION.md` commiteado decía «0 sin trackear»** porque se generó en el worktree; en `main` hay **11** | `git diff docs/sdd/RECONCILIACION.md` | **nuevo** |
| **D4** | `config.yaml` → `decision/veto-plan-r01-1` cita **E-016**, que no existe en `ENTRADA.md` | `grep -n "E-016" docs/sdd/ENTRADA.md` → sólo la nota de este corte | **nuevo** |
| **D5** | **S38 vencida:** F1A-03, 06, 07 y 09 sin empezar | `git log --since=2026-09-11 --oneline \| grep -E "F1A-0(3\|6\|7\|9)"` = 0 | **empeora** |
| **D6** | 11 ficheros de `docs/sdd` sin trackear (eran 12), más `openspec/changes/F0-05/` | `git status --porcelain docs/sdd/` | vivo |
| **D7** | 10 claves `decision/*` en `config.yaml` sin fila en §4.5 (eran 7: +`veto-plan-r01-1` y las dos de dato de este corte, que no son gates) | `comm -23` de `grep -oE "decision/[a-z0-9-]+"` sobre ambos | vivo |
| **D8** | 5 IV vivos (IV-2, 8, 9, 11, 12); 3 sin destino (9, 11, 12); IV-8 con destino **propuesto** F1B-11 | sección 4 de `RECONCILIACION.md` | mejora |
| **D9** | Cifras ancladas: esperas **11 / 4** (legítima) · estados **21** · transiciones **34** · pasos del mapa, reconcile **no lee código** (38 del maestro, sin contraste) | `grep -cE "^  \{ id: '" packages/shared/src/transitions.ts` · `packages/shared/src/estados.ts:59-106` | 3 ✅ · 1 sin medir |
| **D10** | Worktrees `prunable`: **observado**, probablemente porque esta máquina no ve `C:/dev/…-worktrees`. No medido desde Windows | `git worktree list` | vivo |

**Cerrado desde el 18/09:** `.git/index.lock` ya no está · 0 commits sin empujar · retroajuste R-1 hecho (14 cabeceras) · 0 `fuera-del-plan` sin motivo (5 declarados) · capacidades 18 declaradas / 10 specs / 0 huérfanas.

## 4 · Bandeja

Entran **E-017 a E-020** (cerradas). E-003, E-013 y E-014 llevan adenda. Siguen esperando decisión de Gerencia: **E-003, E-004, E-005, E-008 y E-013**. El «espejo de Zoho» (E-018) se queda **sin destino a propósito**: es condicional. Preguntas nuevas en el panel: `e013b-copia-pruebas` y `titularidad-mantenedor`.

## 5 · Sin commitear, para el presupuesto del ledger de la próxima tanda

Modificados: `openspec/config.yaml` (+153), el plan §4.5 y 6 líneas fuera de §4.5, `docs/sdd/ENTRADA.md`, `docs/sdd/R08.3_Expediente_de_cambios.md` (§11 y B.3), `docs/sdd/RECONCILIACION.md` (regenerado). Sin trackear, además: este parte y `F0-05_Mecanismo_de_Reconciliacion.md:93`. Nada de código de producto ni de `openspec/changes/`.
